# Generated by `gw apply`. Re-applies the tunnel exemption after a restart:
# nftables resolves the cgroup path to an ID when the rule is added, and that
# ID changes every time the unit's cgroup is recreated.
[Service]
ExecStartPost=-/usr/local/lib/gateway/ts-bypass.sh exceptions on
