# Local machine state — never commit
gateway.toml
build/
secrets/
*.bak
__pycache__/

# Outbound definitions hold server credentials — only the *.example.json
# templates are committed.
outbounds/*.json
!outbounds/*.example.json
